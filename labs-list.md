# systemd

- create new script to print "hello linux" every 5 seconds
- create a systemd service to run the script
  - a service should be created in `/etc/systemd/system/hello.service`
- start the service and check the output
- make sure the service starts on boot